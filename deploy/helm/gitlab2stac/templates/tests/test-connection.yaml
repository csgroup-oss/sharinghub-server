apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gitlab2stac.fullname" . }}-test-connection"
  labels:
    {{- include "gitlab2stac.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gitlab2stac.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
